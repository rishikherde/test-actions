from setuptools import setup, find_packages

setup(
      name="data_processor",
      version="0.1.0",
      packages=find_packages()
      install_requires=[],
      python_requires='>=3.6',
      )
